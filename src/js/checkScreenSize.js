export const checkScreenWidth = () => {
  const width = window.innerWidth;
  return width;
};

export const checkScreenHeight = () => {
  const height = window.innerHeight;
  return height;
};
